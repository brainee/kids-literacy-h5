---
description: 本仓库 Agent 强制流程（docs 分层、任务看板、测试门禁）
alwaysApply: true
---

# kids-literacy-h5 Agent 规则

- 先读 `AGENTS.md` 与 `docs/dev/sdlc/TASKS.md`，再改代码。
- 产品行为 → `docs/product/specs/`；工程/架构 → `docs/dev/`；调研 → `docs/research/`。
- **主代码**在 `packages/web`（Vite+React+TS）；1.x 遗留在 `packages/web/legacy` + 根 `legacy.html`。
- `packages/` 预留未来多项目；当前不要再拆多包。
- 勿擅自引入后端 / 云 TTS / 账号体系。
- 本地：`cd packages/web && npm run dev`（根目录无 package.json，等有第二项目再加 workspace）。
- 完成前写 `docs/dev/evidence/`，更新 TASKS；仅 Owner 要求时 commit/push。
